const Duration = ({ duration }) => {
    return (
        <div>
            How much time do you want to spend on this activity? Weekend, Day,
            evening, morning, 60 mins, 30 mins
        </div>
    );
};

export default Duration;
